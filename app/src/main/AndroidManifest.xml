<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.strooplocker">

    <!-- (Optional) If you need to declare specific SDK versions -->
    <uses-sdk
        android:minSdkVersion="21"
        android:targetSdkVersion="33" />

    <!-- (Optional) If your app needs extra permissions, declare them here.
         For example:
         <uses-permission android:name="android.permission.INTERNET" />
    -->

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:theme="@style/Theme.StroopLocker">

        <!-- 1) Activity that does NOT have an intent filter, but we want it exported -->
        <activity
            android:name=".SelectAppsActivity"
            android:exported="true"
            android:label="@string/app_name" />

        <!-- 2) Main StroopLockActivity with an intent-filter -->
        <activity
            android:name=".StroopLockActivity"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:exported="true"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>


        <!-- 3) AccessibilityService must specify android:exported explicitly.
               Because it has an intent-filter, we use 'true' here if you want
               the system to bind it from outside your app. -->
        <service
            android:name=".StroopAccessibilityService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- 4) Device Admin receiver also must have android:exported
               since it has an intent filter. -->
        <receiver
            android:name=".StroopDeviceAdminReceiver"
            android:label="@string/app_name"
            android:description="@string/app_description"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
